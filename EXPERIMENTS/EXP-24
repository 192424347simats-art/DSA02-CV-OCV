import cv2

img = cv2.imread("image.jpg")

blur = cv2.GaussianBlur(img, (5,5), 0)

A = 2
result = cv2.addWeighted(img, A, blur, -(A-1), 0)

cv2.imshow("High Boost", result)
cv2.waitKey(0)
cv2.destroyAllWindows()
