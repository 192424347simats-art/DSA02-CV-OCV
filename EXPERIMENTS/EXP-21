import cv2
import numpy as np

img = cv2.imread("image.jpg")

kernel = np.array([[1,1,1],
                   [1,-8,1],
                   [1,1,1]])

lap = cv2.filter2D(img, -1, kernel)
result = cv2.subtract(img, lap)

cv2.imshow("Sharpened", result)
cv2.waitKey(0)
cv2.destroyAllWindows()
